import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

export default function RepoGraph() {
  return (
    <Card sx={{ marginBottom: 2 }}>
      <CardContent>
        <Typography variant="h6">Repository Graph</Typography>
        <Typography variant="body2">[Graph visualization coming soon]</Typography>
      </CardContent>
    </Card>
  );
}
